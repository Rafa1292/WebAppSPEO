@model IEnumerable<WebApplication1.Models.HouseFeature>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Content.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Content.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HouseFeatureId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HouseFeatureId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HouseFeatureId })
        </td>
    </tr>
}

</table>
